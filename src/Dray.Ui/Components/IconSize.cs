namespace Dray.Ui.Components;

public enum IconSize
{
    /// <summary>14px — inside a dense table cell.</summary>
    Sm,

    /// <summary>16px — the default: buttons, inline labels.</summary>
    Md,

    /// <summary>20px — sidebar rows, panel headings.</summary>
    Lg,

    /// <summary>28px — empty states.</summary>
    Xl,
}
