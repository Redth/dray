@page "/volumes"

<PageChromeScope Chrome="@(new PageChrome("Volumes"))" />

<div class="page">
    <EmptyState Icon="IconRef.Volume" Title="Volumes arrive in phase 4" Body="Including browsing what is actually inside a volume — the thing no other Docker GUI does today." />
</div>
