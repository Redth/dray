@page "/stacks"

<PageChromeScope Chrome="@(new PageChrome("Stacks"))" />

<div class="page">
    <EmptyState Icon="IconRef.Stack" Title="Compose stacks arrive in phase 5" Body="Dray will discover running stacks from their compose labels, and track compose files you add so a stack that is down stays visible." />
</div>
