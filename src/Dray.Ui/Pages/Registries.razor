@page "/registries"

<PageChromeScope Chrome="@(new PageChrome("Registries"))" />

<div class="page">
    <EmptyState Icon="IconRef.Registry" Title="Registries arrive in phase 6" Body="Sign in through the platform credential helper, so Dray and the Docker CLI share one credential store and nothing lands in config.json as plaintext." />
</div>
