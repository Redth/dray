@page "/hosts"

<PageChromeScope Chrome="@(new PageChrome("Hosts"))" />

<div class="page">
    <EmptyState Icon="IconRef.Host" Title="Hosts arrive in phase 2" Body="Every Docker context — a local socket, a WSL2 distro, or SSH to a box in a closet — treated exactly the same as this machine." />
</div>
