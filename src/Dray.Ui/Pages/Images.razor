@page "/images"

<PageChromeScope Chrome="@(new PageChrome("Images"))" />

<div class="page">
    <EmptyState Icon="IconRef.Image" Title="Images arrive in phase 4" Body="Layer history, pull with per-layer progress, and a prune that previews exactly what it will reclaim before you confirm." />
</div>
